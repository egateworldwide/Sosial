{
  "name": "@sosial/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared source of truth for Sosial: channel ids, plan entitlements, pure rule helpers. Dependency-free so mobile, web and worker can all consume it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
