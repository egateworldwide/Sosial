/**
 * Bump this on every fix commit while debugging connectivity.
 * Shown in the Connect screen footer so a screenshot proves
 * which bundle a phone is actually running — no more guessing
 * whether Expo Go served fresh code or a cached build.
 */
export const BUILD_TAG = 'b36';
