{
  "name": "sosial-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Sosial publish worker (Railway): claims jobs from Supabase job_queue and runs them. Dependency-free — plain Node 18+ fetch, no pg driver.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@types/node": "^26.6.1"
  }
}
